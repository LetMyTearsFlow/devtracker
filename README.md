# devTracker

一个轻量、纯命令行的开发工作追踪工具，可：

- 🔍 扫描本机目录下所有 Git 仓库并加入追踪列表（scan）
- 📊 以表格形式展示项目路径与 Git 状态（show）
- ⏱ 交互式选择项目并启动计时器，结束后记录笔记（start）
- 📈 统计本周工作总时长并展示最近 5 条日志（stats）
- 📝 记录每个项目的专注时长和简短笔记，数据本地保存为 JSON

适合用来回答：**“我在这个项目上花了多久？做了什么？”**

---

## 安装与运行（uv）

> 依赖已在 `pyproject.toml` 中定义：keyboard / readchar / rich / typer

1. 克隆仓库

```bash
git clone <your-repo-url>
cd devtracker
```

2. 同步依赖
```bash
uv sync
```
3. 运行命令
```bash
uv run python main.py scan "/path/to/your/projects"
uv run python main.py show
uv run python main.py start
uv run python main.py stats
```

# CLI 交互说明（start 模式）

`↑ / ↓`：切换项目

`Enter`：开始计时

`q`：结束计时并保存 session

结束后将自动打开外部编辑器输入笔记并存储：

| 字段                 | 说明           |
| ------------------ | ------------ |
| `project`          | 项目名          |
| `start_time`       | 开始时间（ISO 格式） |
| `duration_seconds` | 本次专注时长（秒）    |
| `note`             | 用户笔记         |

# 目录结构

```
devTracker/
├─ main.py
├─ src/
│  ├─ manager.py      # Git 仓库扫描与状态检查
│  ├─ storage.py      # JSON 读写存储
│  ├─ timer.py        # 计时器逻辑
│  └─ note.py         # 外部笔记输入
└─ data/
   └─ data.json
```